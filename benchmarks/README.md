# Benchmarks

Goal 3 of `SCOPE.md`. Not yet implemented.

The first benchmark must reproduce the manual experiment: warm cache, cold
cache, RAM-constrained and TierInfer-enabled runs of the same model, each
recording throughput, NVMe bandwidth, read-size distribution, IOPS and tier
residency.
