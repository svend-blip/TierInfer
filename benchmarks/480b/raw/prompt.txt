You are reviewing a storage subsystem for a local inference engine. The engine runs a sparse mixture-of-experts model whose weights do not fit in system memory, so expert weights are read from an NVMe RAID array during token generation. Explain, step by step, how you would design an expert cache that decides which experts to keep resident, how you would measure whether prefetching helps, and which failure modes must never corrupt the output. Be concrete about data structures, the metrics you would record per token, and how you would tell an improvement from measurement noise.
